name: slack-table
version: 0.1.0
description: 발신 직전에 Slack Block Kit 으로 다시 쓴다 — 마크다운 표는 data_table 로, SOUL.md 의 인사 응답은 고정 블록으로. 둘 다 아닌 메시지는 건드리지 않는다.
author: platform
kind: standalone
provides_hooks:
  - on_session_start
